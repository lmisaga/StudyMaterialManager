package users;

public interface DisplayInfo {
	/**
	 * Prints info of an actual object of a class which implements 
	 * DisplayInfo interface = primarily used for testing
	 */
	public void ObjectInfo();
}
